# Sales Entry Module

Sales Entry is native inside Billing API and owns sales invoice create, update, suspend, restore, comments, activities, print/PDF/mail handoff, correction/reversal commands, and accounts posting handoff.

Shared framework, tenant context, document numbering, mail, and entry-helper services remain transitional dependencies from the compatibility backend until the shared Billing API infrastructure phase.
