# Purchase Receipt Module

Purchase Receipt is native inside Billing API and owns inbound stock document routes linked to billing workflows.

Shared framework, tenant context, document numbering, and queue providers remain transitional dependencies from the compatibility backend until Billing API receives its own shared infrastructure boundary.
