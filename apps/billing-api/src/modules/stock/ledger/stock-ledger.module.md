# Stock Ledger Module

Stock Ledger is native inside Billing API and owns billing-linked stock ledger read routes.

Shared framework, tenant context, and queue providers remain transitional dependencies from the compatibility backend until Billing API receives its own shared infrastructure boundary.
