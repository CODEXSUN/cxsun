# Release Rules

- Release requires `npm run check`.
- Release requires changelog entry.
- Release requires version consistency check.
- Release requires deployment notes.
- Release requires rollback notes.
- Release requires verified Docker image build for changed deploy units.
- Release tags must follow the documented version tag format.
- No release may include unrelated dependency upgrades.
