# Verification Rules

Foundation gate:

- `npm run typecheck`
- `npm run lint`
- `npm run test`
- `npm run build`
- `npm run check:deps`
- `npm run check:versions`
- `npm run check:docs`
- `npm run check`

Run the smallest relevant subset during intermediate work. Final task completion requires the full gate or documented reason for skipped checks.
